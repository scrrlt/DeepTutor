============================================

DeepTutor Development & CI Dependencies

============================================

Install: pip install -r requirements-dev.txt

Base runtime deps

-r requirements.txt

============================================

Synthetic PDF generation (CI only)

============================================

These libs are forbidden in production images

reportlab>=3.6
Pillow>=9.0

============================================

Development tools

============================================

pre-commit>=3.0.0
pytest>=7.0.0